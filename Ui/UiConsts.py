# Color pair dictionary
color_pairs = {
    "default": 0,
    "window_selected": 1,
    "item_selected": 2,
}

# Symbols representing that element was too big and was truncated
trunc_string = '…'
